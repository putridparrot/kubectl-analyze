## This is under development

# kubectl-analyze

Analysis of kubernetes resources using rules.

```aiignore
kubectl analyze -r my-rules.json -n dev
kubectl analyze -n dev
```

A default set of rules is included in the rules.json file.
